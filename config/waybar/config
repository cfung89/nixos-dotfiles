{
    "modules-left": [ "sway/workspaces" ],
    "modules-center": [ "clock" ],
    "modules-right": [ "wireplumber", "network", "bluetooth", "memory", "cpu" ],

    "sway/workspaces": {
        "format": "{name}: {icon}",
        "format-icons": {
            "focused": "🟢",
            "default": "⚪",
        },
    },

    "clock": {
        "timezone": "America/Montreal",
        "tooltip": false,
        "format": "{:%I:%M:%S %p -  %A, %d %h %Y}",
        "interval": 1,
    },

    "cpu": {
        "interval": 10,
        "format": "CPU | {load} | {usage}%",
    },

    "memory": {
        "interval": 30,
        "format": "Mem | {percentage}% | {used:0.1f} GB / {total:0.1f} GB",
    },
}
